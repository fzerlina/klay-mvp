# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
*.tsbuildinfo

# Vite cache (inside node_modules, but also possible at root)
.vite/

# Environment files — never commit secrets
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / OS clutter
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Editor configs (personal — uncomment to share)
# .vscode/
# .idea/

# Test coverage
coverage/
.nyc_output/

# Claude Code local-only settings (project-shared config in .claude/launch.json
# stays tracked; per-user permissions / drift do not)
.claude/settings.local.json
.claude/worktrees/

# Design handoff bundles — local-only references, not source
.handoff/
